package com.smartmealplanner.recipe;

/** Structured blocking or warning outcome from a Recipe import preflight. */
public enum RecipeImportIssueType {
    INVALID_DOCUMENT,
    BLANK_DATASET,
    BLANK_DATASET_VERSION,
    NO_RECIPES,
    DUPLICATE_SOURCE_IDENTIFIER,
    DUPLICATE_SOURCE_REFERENCE,
    DUPLICATE_SLUG,
    BLANK_TITLE,
    INVALID_SUMMARY,
    INVALID_SERVINGS,
    INVALID_MINUTES,
    INVALID_DIFFICULTY,
    INVALID_SOURCE,
    INVALID_SOURCE_REFERENCE,
    DUPLICATE_INGREDIENT,
    DUPLICATE_INGREDIENT_LINE,
    NON_CONTIGUOUS_INGREDIENT_LINES,
    QUANTITY_UNIT_MISMATCH,
    INVALID_QUANTITY,
    UNKNOWN_INGREDIENT,
    UNKNOWN_UNIT,
    INVALID_PREPARATION_NOTE,
    INVALID_SECTION_LABEL,
    DUPLICATE_STEP,
    NON_CONTIGUOUS_STEPS,
    BLANK_STEP_INSTRUCTION,
    INVALID_STEP_DURATION,
    DUPLICATE_TAG,
    UNKNOWN_TAG,
    INVALID_TAG_REFERENCE,
    DUPLICATE_MEAL_SLOT,
    UNKNOWN_MEAL_SLOT,
    SOURCE_REFERENCE_CONFLICT,
    SLUG_CONFLICT,
    USER_CREATED_CONFLICT,
    INCOMPLETE_NUTRITION
}
